// equality narrowing is using the == with if else conditions


function seomDemo(x:string, y:string){
    if(x == y){// check if the tqo parameters are the same data type
        x.toUpperCase();
    }

}


seomDemo(3, "5") //